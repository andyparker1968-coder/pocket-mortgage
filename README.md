# Pocket Mortgage standalone planner

Open `index.html` directly in a browser. It is a self-contained mortgage
planner with no Node.js, npm, server, or internet connection required.

Your figures are saved in the browser's local storage on this device. The
projection is an estimate for planning and is not financial advice.